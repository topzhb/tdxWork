#!/usr/bin/env python
# -*- coding: utf-8 -*-
"""
tool_config.py — 飞龙在天板块分析 · 项目专属配置
==================================================
通用路径（VIPDOC_DIR, DB_FILE 等）从根目录 tdx_config.py 导入，
本文件只存放项目专属配置（选股参数、板块预设等）。

本文件在 .gitignore 中，不会被提交到 GitHub。
"""

import os
import sys

# 将根目录加入搜索路径，以便导入 tdx_config
sys.path.insert(0, os.path.join(os.path.dirname(os.path.abspath(__file__)), ".."))
from tdx_config import TDX_DIR as VIPDOC_DIR, DB_FILE, CONCEPT_FILE  # noqa: E402

# ============================================================
# 项目专属路径
# ============================================================

BASE_DIR   = os.path.dirname(os.path.abspath(__file__))
OUTPUT_DIR = os.path.join(BASE_DIR, "ConceptReport")

# ============================================================
# 选股公式参数
# ============================================================
MIN_BARS       = 24    # 最小周线K线数
MIN_DAY_BARS   = 50    # 最小日线数据量（不足则跳过）
ANGLE_THRESH   = 63    # 妖线角度阈值（度）
EMA_PERIOD_X1  = 9     # X1 = EMA(Close, 9)
EMA_PERIOD_ZS1 = 5     # ZS1 = EMA(X1 * 1.14, 5)
ZS1_MULT       = 1.14  # 主升1系数

# ============================================================
# 常用板块列表（可按需增删）
# 格式：{ 板块名称: 板块代码 }
# ============================================================
SECTOR_PRESETS = {
    # ---------- 科技/芯片 ----------
    "CPO概念":   "880656",
    "存储芯片":  "880672",
    "芯片":      "880952",

    # ---------- 新能源 ----------
    "固态电池":  "880742",

    # ---------- 能源/基建 ----------
    "智能电网":  "880520",

    # ---------- 其他常用（备用，需要时取消注释）----------
    # "5G概念":   "880506",
    # "低空经济": "880522",
    # "碳中和":   "880519",
    # "黄金概念": "880521",
    # "国防军工": "880507",
    # "稀缺资源": "880505",
}

# "全部板块" 导出时使用（从数据库读取，不受此列表限制）
EXPORT_ALL_SECTORS = False
