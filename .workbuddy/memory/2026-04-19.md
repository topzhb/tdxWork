# 2026-04-19 工作记录

## 发布版本整理（tdxWork 两个项目）

完成了将 tdx-analysis 和 Quan-Select 整理为第三方可共享发布版本的工作：

### 主要变更
1. **脱敏**：清理所有 `c:/Users/T470s/`、`E:\workClaw\`、`e:/workClaw/` 个人路径（覆盖 15+ 文件）
2. **集中配置**：为 Quan-Select 新建 `qs_config.py`，将 fund_strategies.py（6处）、collect.py、score.py、backtest.py 中的硬编码通达信路径统一迁移至此文件
3. **tdx-analysis/tool_config.py**：补充了通达信路径配置注释说明
4. **新增文件**：两个项目各创建 README.md 和 requirements.txt；创建 RELEASE_NOTES.md 发布说明
5. **.gitignore**：tdx-analysis 新增排除 Archive/；Quan-Select 修正 __temp/ 覆盖规则

### 关键文件
- `e:/tdxWork/Quan-Select/qs_config.py`：新增，第三方首次部署只需改此文件
- `e:/tdxWork/RELEASE_NOTES.md`：完整发布说明，含第三方部署步骤
